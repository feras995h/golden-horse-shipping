# Backend Production Environment Example
NODE_ENV=production
PORT=3001

# Database - Cloud PostgreSQL
# Option 1: Use DATABASE_URL (recommended for cloud databases)
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# Option 2: Use individual variables (fallback)
# DB_TYPE=postgres
# DB_HOST=your-cloud-postgres-host.com
# DB_PORT=5432
# DB_USERNAME=your-db-username
# DB_PASSWORD=your-db-password
# DB_NAME=your-db-name
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false

# Set true ONLY for first deployment to auto-create tables (no migrations yet)
DB_SYNCHRONIZE=false

# JWT
JWT_SECRET=please-change-in-prod
JWT_EXPIRES_IN=7d

# CORS - set to your production domain
FRONTEND_URL=https://goldenhorse.ly/

# Uploads
UPLOAD_PATH=/app/uploads
MAX_FILE_SIZE=10485760

# Optional APIs
AIS_API_KEY=
AIS_API_URL=https://api.vesselfinder.com/

# Email (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
