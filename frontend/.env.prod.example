# Frontend Production Environment Example
NODE_ENV=production
# Backend API URL in production (Coolify, separate subdomain)
NEXT_PUBLIC_API_URL=https://api.goldenhorse.ly/api
# If using reverse proxy on same domain instead, you can set:
# NEXT_PUBLIC_API_URL=/api

