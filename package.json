{
  "name": "golden-horse-shipping",
  "version": "1.0.0",
  "description": "Golden Horse Shipping - Complete shipping management system with PostgreSQL support",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "heroku-postbuild": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "cd backend && node dist/main.js",
    "start:prod": "cd backend && node dist/main.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "docker:build": "docker-compose build",
    "docker:start": "docker-compose up -d",
    "docker:stop": "docker-compose down",
    "test": "npm run test --prefix backend",
    "migrate": "npm run migration:run-prod --prefix backend",
    "test-db": "npm run test-db --prefix backend"
  },
  "keywords": [
    "shipping",
    "logistics",
    "postgresql",
    "docker",
    "nestjs",
    "nextjs"
  ],
  "author": "Golden Horse Shipping",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/feras995h/golden-horse-shipping.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "bcrypt": "^6.0.0",
    "pg": "^8.16.3"
  }
}
